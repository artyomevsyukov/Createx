const swiper = new Swiper(".swiper", {
    loop: true,
    slidesPerView: 3,
    spaceBetween: 30,
    // loop: true,
    // slidePerGroup: 2,
    navigation: {
        prevEl: ".portfolio-section__prev",
        nextEl: ".portfolio-section__next",
    },
});
